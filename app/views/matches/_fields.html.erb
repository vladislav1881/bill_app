<%= render 'shared/error_messages', object: f.object %>

<%= f.label t(:invited_id) %>
<%= f.collection_select :invited_id, current_user.followed_users, :id, :name %>

<%= f.label t(:status) %>
<%= f.select :status, %w{ planned playing finished }.map { |s| t("statuses.#{s}") } %>

<%= f.label t(:wins) %>
<%= f.number_field :wins %>

<%= f.label t(:loses) %>
<%= f.number_field :loses %>


t("statuses.#{m.status}")
