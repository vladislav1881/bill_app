<div class="row">
  <div class="span6 offset3" id="newm">
    <%= form_for(Match.new) do |f| %>

      <%= render 'fields', f: f %>
   
      <%= f.submit 'Save', class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>
