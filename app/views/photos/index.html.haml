%h1 Фотографии

.actions(style="margin-bottom:15px")
  = link_to 'Новая фотография', new_photo_path, class: 'btn'

%ul.thumbnails
  - @photos.each do |photo|
    %li.span2
      = link_to image_tag(photo.image.url(:medium_square)), photo

